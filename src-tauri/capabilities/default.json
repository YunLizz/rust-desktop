{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "锦书主窗口默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default"
  ]
}
